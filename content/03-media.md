---
# SCREENS — the image strip.
# Each "## " is one image. The first one spans the full width; the rest sit two-up.
#
#   file:    a filename inside assets/img/
#   alt:     description for screen readers and for when an image fails to load
#   caption: the little amber tag. Leave it OUT for real screenshots — it exists
#            to mark placeholders. Add the line back only if you want a label.
#
# The .webp files are compressed copies of the .png originals you dropped in
# (3.5 MB -> 344 KB, no visible loss). Point "file" at the .webp, not the .png.

eyebrow: 02 · The ship, the map, docking
heading: Screens
---

# Order matters: the FIRST one spans the full width, so lead with the shot that
# best explains the game. screenshot-01 is also the hero image up top, so it is
# placed last here to keep the repeat as far from the hero as possible.

## Solar map and console
file: screenshot-02.webp
alt: The 2D solar navigation map alongside the ship systems console

## Docking
file: screenshot-03.webp
alt: Close-quarters docking approach in the 3D bubble

## Ship near Mars
file: screenshot-01.webp
alt: The MORSS Mk III under way near Mars
